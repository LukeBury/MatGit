function [ yf ] = matmult(x,y)
% This code is not finished

nx = size(x);
ny = size(y);

i = 0;
j = 0;

if nx == ny
    r = nx(1);
    c = nx(2);
    
    yf = zeros(r,c);
    
%     for i=1:r
%         for j=1:c
%             yf(i,j) = x(i,j) + y(i,j);

end


